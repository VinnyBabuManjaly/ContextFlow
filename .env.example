# Required if using Gemini embeddings or LLM (default provider)
GEMINI_API_KEY=

# Required if using OpenAI embeddings or LLM
# OPENAI_API_KEY=

# Redis (defaults work for local docker-compose)
# REDIS_URL=redis://localhost:6379
